using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class TerrainManager : MonoBehaviour
{
    public GameObject Plane;
    public GameObject Ground;
    public GameObject Nature;
    public Material Grass;

    // Start is called before the first frame update
    void Start()
    {
        /*
        Grass.mainTextureScale = new Vector2(8.5f, 8.5f);
        Plane.transform.localScale = new Vector3(1.7f, 1, 1.7f);
        Ground.transform.localScale = new Vector3(17, 3, 17);
        
        Grass.mainTextureScale = new Vector2(7.5f, 7.5f);
        Plane.transform.localScale = new Vector3(1.5f, 1, 1.5f);
        Ground.transform.localScale = new Vector3(15, 3, 15);*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
